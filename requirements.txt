pyrofork==2.3.69
TgCrypto==1.2.5
motor==3.5.1
pymongo==4.8.0
python-dotenv==1.0.1
deep-translator==1.11.4
Pillow==10.4.0
aiohttp==3.10.5
uvloop==0.20.0 ; sys_platform != "win32"
emoji==2.12.1
# yt-dlp intentionally unpinned: it needs frequent updates to keep up with
# extractor changes on YouTube/etc. Pinning this would go stale fast -
# `pip install --upgrade yt-dlp` periodically is expected maintenance.
yt-dlp
